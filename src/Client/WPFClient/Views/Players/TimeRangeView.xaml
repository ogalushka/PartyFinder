<UserControl x:Class="WPFClient.Views.Players.TimeRangeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFClient.Views.Players" xmlns:players="clr-namespace:WPFClient.ViewModel.Players" d:DataContext="{d:DesignInstance Type=players:TimeRangeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="40">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding Day}" HorizontalAlignment="Center" />
        <TextBlock Text="{Binding StartTime}" HorizontalAlignment="Center" />
        <TextBlock Text="{Binding EndTime}" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>
